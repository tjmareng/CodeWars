/*
 * Author: Tyler Marenger
 * Username: tjmareng 
 * Profile: https://www.codewars.com/users/tjmareng
 * Challenge: https://www.codewars.com/kata/opposite-number
 */
function opposite(number) {

    // If the number is negative 
    if (number < 0) {

        // Return positive
        return positiveNum = (number < 0) ? number * -1 : number;
    }
    // Otherwise
    else {

        // Return negative
        return negativeNum = (number < 0) ? number : number * -1;
    }

}